#!/bin/bash

function hello()
{
  echo "Hello, $SLURM_ARRAY_TASK_ID!\n"
}

for s in FHS KORA Malmo QMDiab
do
  sbatch --job-name=$s --array=1 -c 1 -e $s.e -o $s.o --wrap="$HOME/SomaLogic/format.sh $i"
done
squeue -u jhz22
